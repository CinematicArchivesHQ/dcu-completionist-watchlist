export type EpisodeMetadata = { episodeTitle?: string; releaseDate?: string; genres?: string[]; cast?: string[]; description?: string };
export const episodeMetadata: Record<string, EpisodeMetadata> = {};
